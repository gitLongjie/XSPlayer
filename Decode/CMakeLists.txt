
#aux_source_directory(${CMAKE_CURRENT_LIST_DIR} DIR_SRCS)
FILE(GLOB_RECURSE DIR_DECODE_SRCS ${CMAKE_CURRENT_LIST_DIR}/*.h ${CMAKE_CURRENT_LIST_DIR}/*.cpp)

set(DECODE_SRC
    ${DIR_DECODE_SRCS}
    PARENT_SCOPE
)

